# Implementation Plan

## Task Summary
- 5 major tasks / 8 sub-tasks
- 全 19 要件 ID をカバー
- 変更ファイル: `lib/template.tsx`、`tests/template.test.tsx`

---

- [ ] 1. スケール係数算出関数の追加
- [x] 1.1 画像短辺を基準としたスケール係数を算出する純粋関数を実装する
  - モジュールスコープに基準短辺の定数（630）を追加する
  - 入力の `width` と `height` のうち小さい方を基準定数で除算してスケール係数を返す
  - 同一入力に対して常に同一値を返す参照透過な実装にする
  - テスト用に `export` として公開し、`_` プレフィックスでテスト専用であることを示す
  - _Requirements: 1.1, 1.2, 1.7, 4.3, 5.1_

- [ ] 2. デザイントークンのスケーリング・クランプ処理を実装する
- [ ] 2.1 スケール係数と画像サイズを受け取り、クランプ済みトークンを返す内部ヘルパーを実装する
  - 余白・タイトルフォントサイズ・ラベルフォントサイズ・アクセントライン寸法のフィールドを持つ型を定義する
  - 各トークンにスケール係数を乗算する
  - 余白は「スケール後の値」「横幅の 25%」「縦幅の 25%」のうち最小値にクランプし、左右合計・上下合計が各辺の 50% を超えないようにする
  - タイトルフォントサイズは 16px 以上、ラベルフォントサイズは 12px 以上、アクセントライン高さは 1 以上、アクセントライン幅は 4 以上をそれぞれ下限として保証する
  - _Requirements: 1.1, 1.3, 1.4, 1.5, 2.2, 3.3, 3.4_

- [ ] 3. タイトル省略関数をスケール済みフォントサイズに対応させる
- [ ] 3.1 タイトル省略関数の第 3 引数にフォントサイズを追加し、定数参照を除去する
  - 関数シグネチャに `fontSize` 引数を追加する
  - 1 行あたりの概算文字数を、追加した `fontSize` 引数を使って算出するよう変更する
  - テキスト幅がゼロ以下の場合は空文字を返すガードを追加する
  - _Requirements: 2.1, 2.3_

- [ ] 4. `renderTemplate` にスケーリングを統合する
- [ ] 4.1 `renderTemplate` の冒頭でスケール係数・スケール済みトークンを算出し、JSX に適用する
  - `renderTemplate` の冒頭でスケール係数を算出する
  - スケーリング・クランプ処理を呼び出してトークンを取得する
  - JSX の各 `style` プロパティをスケール済みトークンの値に置き換える
  - タイトル省略関数の呼び出しにスケール済みフォントサイズを渡す
  - `renderTemplate` の関数シグネチャおよび返り値の型は変更しない
  - 色トークン（背景色・テキスト色・アクセントカラー）はスケーリング対象外のままにする
  - _Requirements: 1.1, 3.1, 3.2, 4.1, 4.2_

- [ ] 5. テストを追加・更新する
- [ ] 5.1 スケール係数算出関数のユニットテストを追加する
  - `width=400, height=210` を入力したとき、スケール係数が `210/630`（≈ 0.333）になることを検証する
  - `width=630, height=630` を入力したとき、スケール係数が `1.0` になることを検証する
  - `width=1200, height=630` を入力したとき、スケール係数が `1.0` になることを検証する
  - _Requirements: 5.2, 5.3_

- [ ] 5.2 既存スナップショットを再生成し、新規サイズのスナップショットを追加する
  - テスト内の `AppConfig` フィールドが現行の `lib/config.ts` 定義と一致するよう修正する（余分フィールドを削除）
  - `width=400, height=210` を入力として `renderTemplate` を呼び出すテストケースを追加する
  - `width=630, height=630` を入力として `renderTemplate` を呼び出すテストケースを追加する
  - `--update-snapshots` オプションで既存スナップショット 5 件を再生成する
  - デフォルトサイズ（1200×630）のスナップショットが従来と視覚的に同等であることを目視で確認する
  - _Requirements: 1.6, 4.3_
